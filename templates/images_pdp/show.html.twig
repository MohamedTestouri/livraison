<!DOCTYPE html>

<title>ImagesPdp</title>

{% block body %}
    <h1>ImagesPdp</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ images_pdp.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ images_pdp.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('images_pdp_index') }}">back to list</a>

    <a href="{{ path('images_pdp_edit', {'id': images_pdp.id}) }}">edit</a>

    {{ include('images_pdp/_delete_form.html.twig') }}
{% endblock %}
