<!DOCTYPE html>

<title>New ImagesPdp</title>

{% block body %}
    <h1>Create new ImagesPdp</h1>

    {{ include('images_pdp/_form.html.twig') }}

    <a href="{{ path('images_pdp_index') }}">back to list</a>
{% endblock %}
